//your JS code here. If required.
let firstInput = prompt("Enter the first variable:");
let firstResult = parseFloat(firstInput) * 2;

// Take input for the second variable
let secondInput = prompt("Enter the second variable:");
let secondResult = parseFloat(secondInput);

// Add the second input to the result of the first input multiplied by 2
let finalResult = firstResult + secondResult;

// Display the final result
alert("The final result is: " + finalResult);